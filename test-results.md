# test-results.md — ผลการทดสอบ (การบ้านที่ 4 / Module 2 sign-off)

**สรุปสุดท้าย:** ✅ ผ่านทั้งหมด 6/6 เคส (จาก 5 ไฟล์เทสต์) — รันล่าสุด **22 กันยายน 2569 เวลาประมาณ 00:15 น.** ด้วย Playwright + Chromium ยิงตรงไปที่เว็บที่ deploy จริง `https://projectpulse2-eb313.web.app`

รันด้วยคำสั่ง:
```bash
npx playwright test --reporter=list
```

## ผลแต่ละตัว

| # | ไฟล์ | ทดสอบอะไร | ผล | เวลา |
|---|---|---|---|---|
| 1 | `tests/01-main-flow.spec.js` | **เส้นทางหลัก** — นิสิตส่งงาน (`task-detail.html`, ปุ่มส่งงานเข้าคิวตรวจ) → อาจารย์เปิด `feedback-queue.html` ต้องเห็นงานที่เพิ่งส่งอยู่ในคิวจริง | ✅ ผ่าน | 3.2s |
| 2 | `tests/02-status-change.spec.js` | **ปุ่มเปลี่ยนสถานะ** — อาจารย์เปิด `review-feedback.html?sub=...` กดปุ่มตัดสินผล "ผ่าน Milestone" ต้องเปลี่ยน `feedback.decision` เป็น `"passed"` จริงและแสดงผลถูกต้อง | ✅ ผ่าน | 3.0s |
| 3 | `tests/03-required-fields.spec.js` | **ช่องบังคับกรอกห้ามบันทึกถ้ากรอกไม่ครบ** — ฟอร์ม `help-request-new.html` เว้นช่องข้อความ (`message`) แล้วกดส่ง ต้อง**ไม่**สร้างเอกสารใหม่และไม่ redirect ไปหน้า list | ✅ ผ่าน | 9.8s |
| 4a | `tests/04-not-logged-in.spec.js` | 🔒 **ไม่ล็อกอินเปิดไม่ได้** — เปิด `help-request-list.html` แบบไม่มี session ต้องถูกเด้งไป `login.html` | ✅ ผ่าน | 1.9s |
| 4b | `tests/04-not-logged-in.spec.js` | 🔒 **ไม่ล็อกอินเปิดไม่ได้** — เปิด `help-request-new.html` แบบไม่มี session ต้องถูกเด้งไป `login.html` | ✅ ผ่าน | 1.6s |
| 5 | `tests/05-second-account-isolation.spec.js` | 🔒 **บัญชีที่สองเปิดของบัญชีแรกไม่ได้** — บัญชี A สร้างคำร้องขอความช่วยเหลือ (Help Request) → ล็อกอินเป็นบัญชี B → เปิด `help-request-list.html` ต้อง**ไม่เห็น**คำร้องของ A | ✅ ผ่าน | 11.8s |

## ประวัติการรัน (สำคัญ — เก็บไว้เป็นหลักฐานกระบวนการ)

**รอบที่ 1 (ก่อนแก้ไข):** ตอนตรวจโค้ดจริงเทียบกับสเปกครั้งแรก (ก่อนเขียนเทสต์) พบว่า `firestore.rules` เดิมอนุญาต "ล็อกอินแล้วอ่าน/เขียนได้ทุกอย่าง" แบบเหมารวม — นิสิตคนที่สองสามารถอ่าน/แก้/ลบคำร้องของคนอื่นได้จริงถ้ายิง Firestore SDK ตรง ๆ ถือเป็น**ช่องโหว่จริง** ไม่ใช่แค่สมมติฐาน แก้แล้วใน `firestore.rules` (เพิ่ม ACL เฉพาะ collection `helpRequests`/`users`) และ `pages/help-request-list.html` (query กรอง `createdByUid` แทนดึงมาทั้งหมดแล้วกรองฝั่งเว็บ)

**รอบที่ 2 (หลังแก้โค้ด, ก่อน deploy):** รันเทสต์ครั้งแรก — เทสต์ #1/#2 ผ่าน แต่ **#3/#4/#5 ติดบล็อกจากโครงสร้างพื้นฐาน** ไม่ใช่โค้ดแอปหรือเทสต์ผิด: เว็บที่ deploy จริงยังไม่มีไฟล์ `help-request-*.html` เลย (ตรวจสอบแล้วได้ 404) เพราะการ rename ฟีเจอร์และการแก้ `firestore.rules` ยังเป็น local change ที่ยังไม่ได้ deploy ขึ้น Firebase Hosting/Firestore — เทสต์เขียนถูกต้องตามสเปกแล้ว แค่รันไม่ได้จนกว่าจะ deploy

**รอบที่ 3 (หลัง `firebase deploy --only hosting` + `firebase deploy --only firestore:rules`):** รันซ้ำ → **ผ่านทั้งหมด 6/6** ยืนยันว่าทั้งฟีเจอร์ Help Request และการแก้ ACL ทำงานถูกต้องบนเว็บจริงแล้ว

## ข้อที่ไม่ผ่าน ณ ตอนนี้

ไม่มี — ผ่านครบทุกเคสในรอบล่าสุด (ดูรายการช่องโหว่ที่ทราบแล้วและยังไม่แก้ (ไม่ใช่จากเทสต์ชุดนี้) ใน [BACKLOG.md](BACKLOG.md))
